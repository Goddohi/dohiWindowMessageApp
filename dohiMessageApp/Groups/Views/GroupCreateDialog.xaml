<Window x:Class="WalkieDohi.Groups.Views.GroupCreateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WalkieDohi.Groups.Views"
        mc:Ignorable="d"
        Title="그룹 만들기"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MinWidth="540"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="Segoe UI, Malgun Gothic"
        UseLayoutRounding="True">
    <Border Margin="12"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource BorderBrushStrong}"
            BorderThickness="1"
            CornerRadius="3"
            Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="300"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="그룹 이름" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,6"/>
                <TextBox x:Name="GroupNameBox" Width="480" Height="30"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="친구 검색" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6"/>
                <TextBox x:Name="SearchBox"
                         Width="480"
                         Height="30"
                         TextChanged="SearchBox_TextChanged"/>
            </StackPanel>

            <ListBox x:Name="FriendListBox"
                     Grid.Row="4"
                     Width="480"
                     SelectionMode="Single">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <EventSetter Event="PreviewMouseLeftButtonUp" Handler="FriendListBox_ItemClick"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel LastChildFill="True">
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Ip}" Style="{StaticResource CaptionTextStyle}"/>
                            </StackPanel>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="본인은 자동 참여됩니다."
                           Style="{StaticResource CaptionTextStyle}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="결과 전체 선택"
                            Margin="0,0,8,0"
                            Width="104"
                            Height="30"
                            ToolTip="조회된 친구를 전체 선택합니다."
                            Click="SelectAll_Click"/>
                    <Button Content="전체 해제"
                            Margin="0,0,8,0"
                            Width="82"
                            Height="30"
                            Click="ClearSelection_Click"/>
                    <Button Content="확인"
                            Margin="0,0,8,0"
                            Width="72"
                            Height="30"
                            Style="{StaticResource RoundedBasicButtonStyle}"
                            IsDefault="True"
                            Click="Ok_Click"/>
                    <Button Content="닫기"
                            Width="72"
                            Height="30"
                            IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
